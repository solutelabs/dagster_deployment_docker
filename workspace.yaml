load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: usercode.dagster.local
      port: 4000
      location_name: "pipeline-x"
  - grpc_server:
      host: usercode.dagster.local
      port: 5000
      location_name: "pipeline-y"
